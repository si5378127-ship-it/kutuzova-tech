# Audit API (server)

This folder contains the Next.js App Router handler for `POST /api/audit`.

It works in local `next dev` / Node hosting, but **GitHub Pages cannot run it**.

See `STATIC_EXPORT.md` for static-export implications and next steps for the form.
