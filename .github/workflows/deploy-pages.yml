# Deploy static site to GitHub Pages
#
# Requirements in the GitHub repo:
# Settings → Pages → Build and deployment → Source: GitHub Actions
#
# For a project site (https://USER.github.io/REPO/):
#   NEXT_PUBLIC_BASE_PATH is set automatically to "/REPO"
# For a user site (https://USER.github.io/) or custom domain later:
#   set repository variable BASE_PATH to empty string, or edit the env below.

name: Deploy to GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install
        run: npm ci

      - name: Build static export
        run: npm run build
        env:
          # Project Pages: /repo-name — change to "" for root user site / custom domain
          NEXT_PUBLIC_BASE_PATH: /${{ github.event.repository.name }}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
